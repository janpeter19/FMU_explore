[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "fmu_explore"
version = "1.1.7"
authors = [
   {name = "Jan Peter Axelsson"}
]
	
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ['src']